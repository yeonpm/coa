void renderCharacters() {
  maru(maruX, maruY, 100 * ratio);
  blueKirby(blueKirbyX, blueKirbyY, 2.5 * ratio); 
  pinkyKirby(pinkyKirbyX, pinkyKirbyY, 5 * ratio); 
  gear(gearX, gearY, 0.5 * ratio);
}
