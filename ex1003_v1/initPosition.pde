void initPosition() {
  // 초기 위치
  maruX = width - 50 * ratio;
  maruY = height - 75 * ratio;
  blueKirbyX = 50 * ratio;
  blueKirbyY = 50 * ratio;
  pinkyKirbyX = 50 * ratio * 3; // 50*ratio+2*50*ratio
  pinkyKirbyY = 50 * ratio;
  gearX = 60 * ratio + 50 * ratio * 4; // 60*ratio+2*50*ratio+2*50*ratio
  gearY = 60 * ratio;
}
